<Window x:Class="UlrikHovsgaardWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UlrikHovsgaardWpf"
        xmlns:viewModels="clr-namespace:UlrikHovsgaardWpf.ViewModels"
        d:DataContext="{d:DesignInstance viewModels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="UlrikHovsgaard Algorithm" Height="764" Width="1284">
    <Grid>
        <Image x:Name="image" HorizontalAlignment="Left" Height="516" Margin="187,36,0,0" VerticalAlignment="Top" Width="1075"/>
        <Label x:Name="label" Content="Current log" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" FontWeight="Bold"/>
        <Label x:Name="label_Copy" Content="Current graph" HorizontalAlignment="Left" Margin="187,10,0,0" VerticalAlignment="Top" FontWeight="Bold"/>
        <ListBox x:Name="listBox" HorizontalAlignment="Left" Height="628" Margin="10,36,0,0" VerticalAlignment="Top" Width="172"/>
        <Button x:Name="btnAutoGenLog" Content="Automatically generate log" ToolTip="Generates random traces based on the current graph and adds them to the log" HorizontalAlignment="Left" Margin="10,672,0,0" VerticalAlignment="Top" Width="172"/>
        <Button x:Name="btnSaveLog" Content="Save current log..." HorizontalAlignment="Left" Margin="10,697,0,0" VerticalAlignment="Top" Width="172"/>
        <GroupBox x:Name="groupBox" Header="Add a log" HorizontalAlignment="Left" Margin="187,557,0,0" VerticalAlignment="Top" Height="160" Width="234">
            <Grid>
                <StackPanel Margin="10,10,10,35">
                    <StackPanel.Resources>
                        <Style TargetType="{x:Type RadioButton}">
                            <Setter Property="Margin" Value="0,4,0,0"/>
                        </Style>
                    </StackPanel.Resources>
                    <RadioButton Content="Hospital log"/>
                    <RadioButton Content="BPI Challenge 2015"/>
                    <RadioButton Content="BPI Challenge 2014"/>
                    <RadioButton Content="Select file..."/>
                </StackPanel>
                <Button x:Name="btnBrowse" Content="Browse..." HorizontalAlignment="Left" Margin="10,110,0,0" VerticalAlignment="Top" Width="99"/>
                <Button x:Name="btnAddLog" Content="Add" HorizontalAlignment="Left" Margin="114,110,0,0" VerticalAlignment="Top" Width="98"/>


            </Grid>
        </GroupBox>
        <GroupBox x:Name="groupBox_Copy" Header="Add new traces (Click an Activity to add it to the trace)" HorizontalAlignment="Left" Margin="678,557,0,0" VerticalAlignment="Top" Height="160" Width="584"/>
        <DataGrid x:Name="dataGrid" HorizontalAlignment="Left" AutoGenerateColumns="False" Margin="426,563,0,0" VerticalAlignment="Top" Height="101" Width="247" ItemsSource="{Binding Activities, UpdateSourceTrigger=PropertyChanged}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Width="70" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Name" Width="Auto" Binding="{Binding Name}"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBox x:Name="textBox" HorizontalAlignment="Left" Height="23" Text="{Binding Path=AddActivityId, UpdateSourceTrigger=PropertyChanged}" Margin="426,669,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="74">
            <TextBox.Style>
                <Style TargetType="TextBox" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                    <Style.Resources>
                        <VisualBrush x:Key="CueBannerBrush" AlignmentX="Left" AlignmentY="Center" Stretch="None">
                            <VisualBrush.Visual>
                                <Label Content=" Id" Foreground="LightGray" />
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Style.Resources>
                    <Style.Triggers>
                        <Trigger Property="Text" Value="{x:Static sys:String.Empty}">
                            <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                        </Trigger>
                        <Trigger Property="Text" Value="{x:Null}">
                            <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="Background" Value="White" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>
        <TextBox x:Name="textBox1" HorizontalAlignment="Left" Height="23" Margin="505,669,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="168">
            <TextBox.Style>
                <Style TargetType="TextBox" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                    <Style.Resources>
                        <VisualBrush x:Key="CueBannerBrush" AlignmentX="Left" AlignmentY="Center" Stretch="None">
                            <VisualBrush.Visual>
                                <Label Content=" Name" Foreground="LightGray" />
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Style.Resources>
                    <Style.Triggers>
                        <Trigger Property="Text" Value="{x:Static sys:String.Empty}">
                            <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                        </Trigger>
                        <Trigger Property="Text" Value="{x:Null}">
                            <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="Background" Value="White" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>
        <Button x:Name="button" Content="Add activity" HorizontalAlignment="Left" Margin="577,697,0,0" VerticalAlignment="Top" Width="96"/>
        <Button x:Name="button1" Content="Reset" HorizontalAlignment="Left" Margin="1187,11,0,0" VerticalAlignment="Top" Width="75"/>
        <Button x:Name="button2" Content="Save current graph..." HorizontalAlignment="Left" Margin="1053,11,0,0" VerticalAlignment="Top" Width="129"/>
        <Button x:Name="button2_Copy" Content="Load current graph..." HorizontalAlignment="Left" Margin="919,11,0,0" VerticalAlignment="Top" Width="129"/>

    </Grid>
</Window>
